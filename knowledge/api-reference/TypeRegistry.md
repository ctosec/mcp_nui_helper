# TypeRegistry

**Namespace:** `Tizen.NUI`
**Inherits:** `BaseHandle`
**Category:** basic

The TypeRegistry allows registration of type instance creation functions.     These can then be created later by name and down cast to the appropriate type.

## Properties

| Name | Type | Description |
|------|------|-------------|
| Instance | `static TypeRegistry` | The TypeRegistry allows registration of type instance creation functions.     These can then be created later by name and down cast to the appropriate type. |

## Methods

| Name | Signature | Description |
|------|-----------|-------------|
| Get | `TypeRegistry Get()` | The TypeRegistry allows registration of type instance creation functions.     These can then be created later by name and down cast to the appropriate type. |
| GetTypeNameCount | `uint GetTypeNameCount()` | Allows the creation of an empty typeRegistry handle. |
| GetTypeName | `string GetTypeName(uint index)` | Gets type names by index. |

