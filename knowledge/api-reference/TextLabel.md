# TextLabel

**Namespace:** `Tizen.NUI.BaseComponents`
**Category:** basic

A control which renders a short text string.<br />     Text labels are lightweight, non-editable, and do not respond to the user input.<br />

## Properties

| Name | Type | Description |
|------|------|-------------|
| TextLabel | `partial class` | A control which renders a short text string.<br />     Text labels are lightweight, non-editable, and do not respond to the user input.<br /> |
| AsyncHeightForWidthComputed | `event EventHandler<AsyncTextSizeComputedEventArgs>` | The AsyncHeightForWidthComputed signal is emitted when the async natural size computed. |
| AsyncNaturalSizeComputed | `event EventHandler<AsyncTextSizeComputedEventArgs>` | The AsyncNaturalSizeComputed signal is emitted when the async natural size computed. |
| AsyncTextRendered | `event EventHandler<AsyncTextRenderedEventArgs>` | The AsyncTextRendered signal is emitted when the async text rendered. |
| AnchorClicked | `event EventHandler<AnchorClickedEventArgs>` | The AnchorClicked signal is emitted when the anchor is clicked. |
| TextFitChanged | `event EventHandler` | An event for the TextFitChanged signal which can be used to subscribe or unsubscribe the event handler         provided by the user. The TextFitChanged signal is emitted when the text fit properties changes.<br /> |

## Events

| Name | Signature | Description |
|------|-----------|-------------|
| AsyncHeightForWidthComputed | `EventHandler<AsyncTextSizeComputedEventArgs>` | A control which renders a short text string.<br />     Text labels are lightweight, non-editable, and do not respond to the user input.<br /> |
| AsyncNaturalSizeComputed | `EventHandler<AsyncTextSizeComputedEventArgs>` | The AsyncNaturalSizeComputed signal is emitted when the async natural size computed. |
| AsyncTextRendered | `EventHandler<AsyncTextRenderedEventArgs>` | The AsyncTextRendered signal is emitted when the async text rendered. |
| AnchorClicked | `EventHandler<AnchorClickedEventArgs>` | The AnchorClicked signal is emitted when the anchor is clicked. |
| TextFitChanged | `EventHandler` | An event for the TextFitChanged signal which can be used to subscribe or unsubscribe the event handler         provided by the user. The TextFitChanged signal is emitted when the text fit properties changes.<br /> |

