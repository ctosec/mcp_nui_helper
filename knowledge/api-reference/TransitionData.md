# TransitionData

**Namespace:** `Tizen.NUI`
**Inherits:** `BaseHandle`
**Category:** basic

This object translates data from a property array of maps into an array of animators.     This is normally used when animating visuals.

## Methods

| Name | Signature | Description |
|------|-----------|-------------|
| Count | `uint Count()` | This object translates data from a property array of maps into an array of animators.     This is normally used when animating visuals. |
| GetAnimatorAt | `PropertyMap GetAnimatorAt(uint index)` | returns the count of the individual property transitions stored within this handle. |

